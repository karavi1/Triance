import AWS, { AWSError } from 'aws-sdk';
import { CredentialsOptions } from '../types';
export default class AwsCredentials extends AWS.Credentials {
    chain: AWS.CredentialProviderChain;
    private hint;
    hintShown: boolean;
    private logHintFn;
    constructor(hintFn: (hint: string) => void);
    refresh(callback: (err?: AWSError) => void): void;
    addConfig(hint: string, credentials?: Partial<CredentialsOptions>): void;
    addEnvironment(hint: string, prefix?: string): void;
    addProfile(hint: string, profile?: string): void;
}
//# sourceMappingURL=index.d.ts.map