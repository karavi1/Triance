import Plugin from 'serverless/classes/Plugin';
import { ServerlessWithCustom } from './types';
export default class ServerlessBetterCredentials implements Plugin {
    hooks: {
        initialize?: () => Promise<void>;
    };
    private provider;
    private serverless;
    constructor(serverless: ServerlessWithCustom);
    init(): Promise<void>;
}
//# sourceMappingURL=ServerlessBetterCredentials.d.ts.map